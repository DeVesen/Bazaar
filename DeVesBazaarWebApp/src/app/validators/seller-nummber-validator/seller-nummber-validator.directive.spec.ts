import { SellerNummberValidatorDirective } from './seller-nummber-validator.directive';

describe('SellerNummberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SellerNummberValidatorDirective(null);
    expect(directive).toBeTruthy();
  });
});
